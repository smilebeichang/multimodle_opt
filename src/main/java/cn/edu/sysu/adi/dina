1.D(A) 的计算方式：
rum  --> K_L information --> D（A）j  --> 求平均 ADI

2.D(B) 的计算方式:
rum  --> K_L information --> D(B)j   --> 加权求平均 ADI

3.D(S) 的计算方式:  S_MOPSO_D
rum  --> K_L information --> D(S)j
解的评价标准:min的ADI = F(sm)
平行试卷：权衡 F(D) + F(S)


汇总：
1. 实现了 RUM 的 D(A) 的计算 （公式之前没理解 rum 的计算由来,以及 D(A)的计算）
2. 模仿 RUM  实现了 DINA,但不知道对不对，因为只是把  底层的计算公式 由 rum 换成了 dina
   变化的地方，还有 参数的设定（rum 是没每道题每道属性，一个惩罚系数；dina 是猜测率 和 猜错率）
3. 以上，两步将数据同步到了数据库，先生成RUM，然后根据id,同步dina,包括各种adij
4. 选择，交叉 计算适应度值,初步实现。  avg min

问题：
1.  dina 的参数
2.  变异  的
